# KRP Vision Docs

Сайт читает канонические Markdown-файлы напрямую из `../design`, `../concept-art` и `../research`. Копий документов внутри сайта нет.

```powershell
cd site
pnpm install
pnpm start
```

Проверка production-сборки:

```powershell
pnpm build
pnpm serve
```
